import { Stack, Button } from '@mui/material';
import { NextPage } from 'next/types';

const Home: NextPage = () => {
  return (
    <div>
      <main></main>
    </div>
  );
};

export default Home;
